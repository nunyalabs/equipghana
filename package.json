{
  "name": "equip-registers",
  "version": "0.1.0",
  "private": true,
  "description": "EQUIP Health Ghana registers PWA - build and deploy tooling",
  "archiveNote": "Legacy/reference files moved to ./archive and excluded from build",
  "scripts": {
    "build": "node build.js",
    "publish": "node publish.js"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.31.6"
  }
}
